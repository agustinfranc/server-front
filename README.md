# Server Project - Frontend

## Build Setup

```bash
# install dependencies
$ npm install

# create .env
set env variables based on .env.example

# serve with hot reload at localhost:3000
$ npm run dev
```
